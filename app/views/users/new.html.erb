<div class="gray_border users">
	<h2>Add a new user</h2>
	<% @user.password = @user.password_confirmation = nil %>
	
	<% form_for :user, :url => users_path do |f| -%>
	<%= f.error_messages%>
	<%= render :partial => 'form', :locals => {:f => f} %>
	<p><%= f.submit 'Sign up' %></p>
	<% end -%>
</div>